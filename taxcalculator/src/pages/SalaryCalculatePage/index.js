import Form from "../../components/Form";
import './index.css';

export default function SalaryCalculatePage() {
    return (
        <div className={"mainPage"}>
            <Form />
        </div>
    );
}